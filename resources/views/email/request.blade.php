@if($name)
    Имя: {{ $name }}
@endif
@if($phone)
    Телефон: {{ $phone }}
@endif
@if($email)
    E-mail: {{ $email }}
@endif
@if($ip)
    IP-адрес: {{ $ip }}
@endif
@if($city)
    Город: {{ $city }}
@endif
